import RegisterForm from "@/components/Register/Register";

export const Register = () => {
  return (
    <main>
      <div>
        <RegisterForm />
      </div>
    </main>
  );
};

export default Register;
